# Template for the dashboard login credentials.
# Copy this file to `secrets.toml` (same folder) and set your own values.
# On Streamlit Community Cloud, paste the filled-in version into the app's
# Settings -> Secrets box (the file itself is never deployed).
[auth]
username = "changeme"
password = "change-this-to-a-strong-password"
